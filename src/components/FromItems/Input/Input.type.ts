export type InputProps = {
  //   label: string;
  name: string;
  type: "text" | "password" | "email" | "number" | "tel" | "checkbox" | "search" | "date" | "time" | "datetime-local" | "month" | "week" | "color";
  // type: string;
  // value: string;
  // onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
  className?: string;
  id?: string;
  // placeholder?: string;
  // required?: boolean;
  // disabled?: boolean;
};
